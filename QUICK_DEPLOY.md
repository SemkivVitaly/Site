# 🚀 Быстрое развертывание в интернете

Эта инструкция поможет вам быстро развернуть приложение на сервере с использованием Docker.

## 📋 Быстрый старт (5 минут)

### 1. Подготовка сервера

**Вариант А: VPS сервер (рекомендуется)**
- Купите VPS у провайдера (Timeweb, DigitalOcean, Vultr и т.д.)
- Минимум: 2GB RAM, 2 CPU cores, 20GB SSD
- Рекомендуется: Ubuntu 20.04 или 22.04

**Вариант Б: Облачная платформа (Railway, Render)**
- Зарегистрируйтесь на [Railway.app](https://railway.app) или [Render.com](https://render.com)
- Следуйте инструкциям на платформе

### 2. Установка Docker (только для VPS)

Подключитесь к серверу по SSH и выполните:

```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установка Docker Compose
apt install docker-compose -y

# Проверка
docker --version
docker-compose --version
```

### 3. Загрузка проекта

```bash
# Создайте директорию
mkdir -p /opt/typography-erp
cd /opt/typography-erp

# Загрузите файлы проекта
# Вариант 1: Через Git (если репозиторий есть)
git clone <ваш-репозиторий> .

# Вариант 2: Через SCP (с вашего компьютера)
# scp -r /путь/к/проекту/* root@your-server-ip:/opt/typography-erp/
```

### 4. Настройка переменных окружения

```bash
# Скопируйте пример файла
cp .env.example .env

# Отредактируйте файл
nano .env
```

**Обязательно измените:**
- `DB_PASSWORD` - придумайте сложный пароль
- `JWT_SECRET` - случайная строка минимум 32 символа
- `QR_POINT_SECRET` - случайная строка минимум 32 символа
- `FRONTEND_URL` - URL вашего сайта (например: `https://yourdomain.com`)

### 5. Запуск приложения

```bash
# Соберите и запустите все сервисы
docker-compose up -d --build

# Проверьте статус
docker-compose ps

# Посмотрите логи
docker-compose logs -f
```

### 6. Инициализация базы данных

```bash
# Войдите в контейнер бэкенда
docker exec -it typography_backend sh

# Выполните миграции
npx prisma migrate deploy

# Создайте администратора
npm run create:admin
# Введите данные администратора когда попросит

# Выйдите
exit
```

### 7. Настройка домена и SSL (опционально, но рекомендуется)

Если у вас есть домен:

```bash
# Установите Nginx и Certbot
apt install nginx certbot python3-certbot-nginx -y

# Создайте конфигурацию
nano /etc/nginx/sites-available/typography
```

Добавьте:

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;

    location / {
        proxy_pass http://localhost:80;
        proxy_set_header Host $host;
    }

    location /api {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
    }

    location /socket.io {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

```bash
# Активируйте
ln -s /etc/nginx/sites-available/typography /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx

# Получите SSL сертификат (бесплатно от Let's Encrypt)
certbot --nginx -d yourdomain.com -d www.yourdomain.com
```

### 8. Готово! 🎉

Откройте браузер и перейдите по адресу:
- `http://your-server-ip` (если нет домена)
- `https://yourdomain.com` (если настроили домен)

## 🔐 Безопасность

Обязательно:
1. ✅ Используйте сильные пароли
2. ✅ Измените все секретные ключи в `.env`
3. ✅ Настройте SSL (HTTPS)
4. ✅ Настройте firewall:

```bash
ufw allow 22/tcp   # SSH
ufw allow 80/tcp   # HTTP
ufw allow 443/tcp  # HTTPS
ufw enable
```

## 📊 Полезные команды

```bash
# Остановить приложение
docker-compose down

# Запустить приложение
docker-compose up -d

# Посмотреть логи
docker-compose logs -f backend
docker-compose logs -f frontend

# Перезапустить контейнер
docker-compose restart backend

# Обновить приложение
docker-compose down
git pull  # или загрузите новые файлы
docker-compose up -d --build
```

## ❓ Проблемы?

**Приложение не запускается:**
```bash
# Проверьте логи
docker-compose logs

# Проверьте порты
netstat -tulpn | grep :5000
netstat -tulpn | grep :80
```

**База данных не подключается:**
- Проверьте правильность `DATABASE_URL` в `.env`
- Убедитесь, что контейнер PostgreSQL запущен: `docker-compose ps`

**Frontend не может подключиться к Backend:**
- Проверьте `FRONTEND_URL` в `.env` бэкенда
- Убедитесь, что бэкенд доступен: `curl http://localhost:5000/api/health`

## 📚 Подробная документация

См. файл `DEPLOY.md` для более подробной информации и альтернативных способов развертывания.

