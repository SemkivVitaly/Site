# PostgreSQL Database
# ВАЖНО: Используйте надежный пароль минимум 16 символов
POSTGRES_PASSWORD=typography_secure_password_2024

# Backend JWT Secret (минимум 32 символа, рекомендуется 64)
# Сгенерируйте случайную строку в PowerShell:
# $jwtSecret = -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | ForEach-Object {[char]$_})
JWT_SECRET=your-jwt-secret-key-minimum-32-characters-long-change-this

# Backend QR Secret (минимум 32 символа, рекомендуется 64)
# Сгенерируйте случайную строку в PowerShell:
# $qrSecret = -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | ForEach-Object {[char]$_})
QR_POINT_SECRET=your-qr-secret-key-minimum-32-characters-long-change-this

# Frontend API URL
# Если у вас есть домен: https://ваш-домен.com/api
# Если домена нет: http://IP-адрес-сервера:8080/api
# Узнать IP: ipconfig (в PowerShell)
REACT_APP_API_URL=http://localhost:8080/api

# Backend Frontend URL (для CORS)
# Если у вас есть домен: https://ваш-домен.com
# Если домена нет: http://IP-адрес-сервера:8080
FRONTEND_URL=http://localhost:8080

# Дополнительные настройки (опционально)
JWT_EXPIRES_IN=7d
MAX_FILE_SIZE=10485760

