# Подробная инструкция по развертыванию проекта на Railway

## Что такое Railway?

Railway - это платформа для развертывания веб-приложений, которая автоматически собирает и запускает ваш код. Она идеально подходит для Node.js приложений с базой данных PostgreSQL.

## Предварительные требования

1. Аккаунт на [GitHub](https://github.com) (если у вас нет, создайте бесплатный)
2. Аккаунт на [Railway](https://railway.app) (можно зарегистрироваться через GitHub)
3. Ваш проект должен быть загружен в GitHub репозиторий

---

## Часть 1: Подготовка проекта для деплоя

### Шаг 1.1: Проверка структуры проекта

Убедитесь, что у вас есть следующие файлы:
- `backend/package.json` - зависимости бэкенда
- `frontend/package.json` - зависимости фронтенда
- `backend/prisma/schema.prisma` - схема базы данных
- `.env.example` или `backend/env.example` - пример переменных окружения

### Шаг 1.2: Создание файлов для Railway

#### Для бэкенда:

Создайте файл `backend/railway.json`:
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

Создайте файл `backend/nixpacks.toml` (опционально):
```toml
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "npm ci",
  "npx prisma generate"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "node dist/index.js"
```

#### Для фронтенда:

Создайте файл `frontend/railway.json`:
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

Создайте файл `frontend/nixpacks.toml`:
```toml
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm ci"]

[start]
cmd = "npm start"
```

### Шаг 1.3: Обновление package.json

#### backend/package.json - добавьте скрипт для продакшена:
```json
{
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "postinstall": "prisma generate"
  }
}
```

#### frontend/package.json - добавьте скрипт:
```json
{
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build"
  }
}
```

### Шаг 1.4: Обновление кода для production

#### backend/src/index.ts - убедитесь, что порт берется из переменной окружения:
```typescript
const PORT = process.env.PORT || 5000;
```

#### frontend/src/api/client.ts - обновите базовый URL:
```typescript
// Должен использовать переменную окружения или определять автоматически
const API_URL = process.env.REACT_APP_API_URL || 'http://localhost:5000';
```

---

## Часть 2: Загрузка проекта на GitHub

### Шаг 2.1: Создание репозитория на GitHub

1. Откройте [GitHub.com](https://github.com)
2. Нажмите кнопку **"+"** в правом верхнем углу
3. Выберите **"New repository"**
4. Заполните:
   - **Repository name**: `typography-erp` (или другое имя)
   - **Description**: "ERP система для типографии"
   - Выберите **Public** или **Private**
   - НЕ добавляйте README, .gitignore или лицензию (если проект уже есть локально)
5. Нажмите **"Create repository"**

### Шаг 2.2: Загрузка кода в GitHub

Откройте терминал в корневой директории проекта и выполните:

```bash
# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Создание первого коммита
git commit -m "Initial commit"

# Добавление удаленного репозитория (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/typography-erp.git

# Загрузка кода на GitHub
git branch -M main
git push -u origin main
```

**Визуально это будет выглядеть так:**
- В терминале вы увидите процесс загрузки файлов
- После завершения обновите страницу GitHub - должны появиться все ваши файлы

---

## Часть 3: Регистрация и настройка Railway

### Шаг 3.1: Регистрация на Railway

1. Откройте [railway.app](https://railway.app)
2. Нажмите кнопку **"Start a New Project"** или **"Login"**
3. Выберите **"Login with GitHub"**
4. Подтвердите доступ Railway к вашему GitHub аккаунту
5. Нажмите **"Authorize Railway"**

**На экране вы увидите:**
- Страницу авторизации GitHub
- Запрос на разрешение доступа Railway
- После подтверждения - главную панель Railway

### Шаг 3.2: Создание нового проекта

1. На главной странице Railway нажмите **"+ New Project"** (зеленая кнопка)
2. В появившемся меню выберите **"Deploy from GitHub repo"**
3. Railway покажет список ваших GitHub репозиториев
4. Найдите и выберите ваш репозиторий `typography-erp`
5. Railway начнет автоматически определять структуру проекта

**На экране вы увидите:**
- Меню с опциями создания проекта
- Список ваших репозиториев из GitHub
- Индикатор загрузки при определении структуры проекта

---

## Часть 4: Настройка базы данных PostgreSQL

### Шаг 4.1: Создание базы данных

1. В вашем проекте на Railway нажмите **"+ New"**
2. Выберите **"Database"**
3. Выберите **"PostgreSQL"**
4. Railway автоматически создаст базу данных

**На экране вы увидите:**
- Кнопку "+ New" в интерфейсе проекта
- Меню с опциями (Database, GitHub Repo, и т.д.)
- После создания - карточку с базой данных

### Шаг 4.2: Получение строки подключения

1. Кликните на созданную базу данных PostgreSQL
2. Перейдите на вкладку **"Variables"**
3. Найдите переменную **`DATABASE_URL`** или **`POSTGRES_URL`**
4. Скопируйте значение этой переменной (это строка подключения к базе данных)

**На экране вы увидите:**
- Страницу базы данных с вкладками (Overview, Variables, Metrics)
- Список переменных окружения
- Значение DATABASE_URL, которое нужно скопировать

---

## Часть 5: Настройка бэкенда

### Шаг 5.1: Добавление бэкенда как сервиса

1. В вашем проекте Railway нажмите **"+ New"**
2. Выберите **"GitHub Repo"** (если еще не добавлен основной репозиторий)
3. Выберите ваш репозиторий
4. Railway автоматически определит, что это Node.js проект
5. **⚠️ КРИТИЧЕСКИ ВАЖНО:** В настройках сервиса укажите:
   - **Root Directory**: `backend` (это обязательно!)
   - **Build Command**: `npm install && npx prisma generate && npm run build`
   - **Start Command**: `npx prisma migrate deploy && npm start`

**Если Root Directory не указан, Railway будет пытаться собрать проект из корня, что приведет к ошибке!**

**На экране вы увидите:**
- Карточку вашего сервиса
- Настройки (Settings) с различными опциями
- Поля для настройки Root Directory и Start Command

### Шаг 5.2: Настройка переменных окружения для бэкенда

1. Кликните на сервис бэкенда
2. Перейдите на вкладку **"Variables"**
3. Добавьте следующие переменные:

```
DATABASE_URL=<значение из PostgreSQL базы данных>
JWT_SECRET=<случайная строка, минимум 32 символа>
JWT_EXPIRES_IN=7d
NODE_ENV=production
PORT=5000
FRONTEND_URL=<будет настроено позже, временно оставьте пустым>
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
QR_POINT_SECRET=<случайная строка для QR кодов>
```

**Как добавить переменную:**
1. Нажмите **"+ New Variable"**
2. В поле **"Name"** введите имя переменной (например, `JWT_SECRET`)
3. В поле **"Value"** введите значение
4. Нажмите **"Add"**

**Генерация случайных строк для JWT_SECRET и QR_POINT_SECRET:**
- Используйте онлайн генератор: https://randomkeygen.com/
- Или в терминале: `openssl rand -base64 32`

### Шаг 5.3: Запуск миграций базы данных

1. В настройках сервиса бэкенда найдите раздел **"Settings"**
2. Перейдите в **"Deploy"**
3. Добавьте **"Build Command"**: `npm install && npx prisma generate && npm run build`
4. Добавьте **"Start Command"**: `npx prisma migrate deploy && npm start`

**Альтернативный способ через Railway CLI:**
1. Установите Railway CLI: `npm i -g @railway/cli`
2. Выполните: `railway login`
3. Выполните: `railway link` (выберите ваш проект)
4. Выполните: `cd backend && railway run npx prisma migrate deploy`

### Шаг 5.4: Получение URL бэкенда

1. В настройках сервиса бэкенда перейдите на вкладку **"Settings"**
2. Найдите раздел **"Networking"**
3. Включите **"Generate Domain"** или создайте кастомный домен
4. Скопируйте сгенерированный URL (например, `backend-production.up.railway.app`)

**На экране вы увидите:**
- Переключатель "Generate Domain"
- После включения - сгенерированный URL
- Кнопку для копирования URL

---

## Часть 6: Настройка фронтенда

### Шаг 6.1: Добавление фронтенда как сервиса

1. В вашем проекте Railway нажмите **"+ New"**
2. Выберите **"GitHub Repo"**
3. Выберите тот же репозиторий
4. **⚠️ КРИТИЧЕСКИ ВАЖНО:** В настройках сервиса укажите:
   - **Root Directory**: `frontend` (это обязательно!)
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`

**Если Root Directory не указан, Railway будет пытаться собрать проект из корня, что приведет к ошибке!**

**Важно:** Для React приложения в production лучше использовать статический хостинг.

### Шаг 6.2: Обновление API URL во фронтенде

Создайте файл `.env.production` в директории `frontend/`:
```
REACT_APP_API_URL=https://ваш-бэкенд-url.railway.app
```

Или настройте через переменные окружения Railway:
1. В сервисе фронтенда перейдите в **"Variables"**
2. Добавьте переменную: `REACT_APP_API_URL=https://ваш-бэкенд-url.railway.app`

### Шаг 6.3: Настройка переменных окружения для фронтенда

В **"Variables"** фронтенда добавьте:
```
REACT_APP_API_URL=https://ваш-бэкенд-url.railway.app
```

### Шаг 6.4: Обновление FRONTEND_URL в бэкенде

1. Вернитесь к сервису бэкенда
2. Перейдите в **"Variables"**
3. Обновите переменную `FRONTEND_URL` на URL вашего фронтенда (будет получен позже)

---

## Часть 7: Настройка статического хостинга для фронтенда (рекомендуется)

### Вариант A: Использование Railway Static (рекомендуется)

1. В настройках сервиса фронтенда:
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: оставьте пустым или используйте nginx
   - **Output Directory**: `build`

2. Railway автоматически определит статические файлы и будет их раздавать

### Вариант B: Использование отдельного сервиса (Netlify, Vercel)

Если Railway не поддерживает статический хостинг хорошо, можно использовать Netlify или Vercel:

#### Для Netlify:
1. Зарегистрируйтесь на [netlify.com](https://netlify.com)
2. Подключите ваш GitHub репозиторий
3. Настройки сборки:
   - **Base directory**: `frontend`
   - **Build command**: `npm install && npm run build`
   - **Publish directory**: `frontend/build`
4. Добавьте переменную окружения: `REACT_APP_API_URL`

#### Для Vercel:
1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Импортируйте проект из GitHub
3. Настройки:
   - **Framework Preset**: Create React App
   - **Root Directory**: `frontend`
4. Добавьте переменную окружения: `REACT_APP_API_URL`

---

## Часть 8: Запуск миграций и инициализация базы данных

### Шаг 8.1: Выполнение миграций базы данных

**⚠️ ВАЖНО:** Миграции автоматически выполняются при каждом деплое благодаря настройке в `backend/nixpacks.toml` (строка `cmd = "npx prisma migrate deploy && node dist/index.js"`). 

Однако, если вам нужно выполнить миграции вручную перед первым запуском, есть несколько способов:

#### Способ 1: Автоматическое выполнение при деплое (рекомендуется)

Миграции выполнятся автоматически при первом деплое бэкенда. Просто разверните сервис бэкенда, и миграции применятся автоматически.

#### Способ 2: Через Railway CLI с публичным DATABASE_URL

**Проблема:** Команда `railway run` с локальной машины не может использовать внутренний адрес базы данных (`postgres.railway.internal`), так как он доступен только внутри инфраструктуры Railway.

**Решение:**

1. Получите публичный `DATABASE_URL` из Railway:
   - Откройте ваш проект на Railway
   - Перейдите к PostgreSQL базе данных
   - Откройте вкладку **"Variables"**
   - Найдите переменную `DATABASE_URL` или `POSTGRES_URL`
   - Скопируйте значение (оно должно содержать публичный адрес, например `postgres://...@containers-us-west-xxx.railway.app:5432/railway`)

2. Установите Railway CLI: `npm i -g @railway/cli`

3. Выполните вход: `railway login`

4. Свяжите проект: `railway link` (выберите ваш проект)

5. Установите публичный DATABASE_URL как переменную окружения:
   ```bash
   railway variables set DATABASE_URL="<ваш-публичный-DATABASE_URL>"
   ```

6. Выполните миграции:
   ```bash
   cd backend
   railway run npx prisma migrate deploy
   ```

#### Способ 3: Использование публичного DATABASE_URL локально

1. Получите публичный `DATABASE_URL` из Railway (см. Способ 2, шаг 1)

2. Создайте временный `.env` файл в `backend/` с этим URL:
   ```env
   DATABASE_URL="<ваш-публичный-DATABASE_URL>"
   ```

3. Выполните миграции локально:
   ```bash
   cd backend
   npx prisma migrate deploy
   ```

4. ⚠️ **ВАЖНО:** После выполнения удалите временный `.env` файл или замените его на локальную конфигурацию для разработки.

#### Способ 4: Через Railway Console (веб-интерфейс)

1. Откройте ваш проект на Railway
2. Перейдите к сервису бэкенда
3. Откройте вкладку **"Deployments"**
4. Кликните на последний деплой
5. Найдите кнопку **"View Logs"** или **"Open Console"**
6. Выполните команду: `npx prisma migrate deploy`

**Рекомендация:** Используйте Способ 1 (автоматическое выполнение при деплое) - это самый безопасный и удобный метод. Миграции уже настроены в `backend/nixpacks.toml` и будут выполняться автоматически.

---

## Часть 9: Настройка кастомного домена (опционально)

### Шаг 9.1: Для бэкенда

1. В настройках сервиса бэкенда перейдите в **"Settings"**
2. Найдите раздел **"Networking"**
3. Нажмите **"Custom Domain"**
4. Введите ваш домен (например, `api.vash-sait.ru`)
5. Railway предоставит DNS записи для настройки
6. Настройте DNS записи у вашего провайдера домена:
   - Тип: `CNAME`
   - Имя: `api` (или `@`)
   - Значение: `ваш-бэкенд.railway.app`

### Шаг 9.2: Для фронтенда

Аналогично настройте домен для фронтенда (например, `vash-sait.ru`)

---

## Часть 10: Обновление кода и автоматический деплой

### Шаг 10.1: Настройка автоматического деплоя

Railway автоматически развертывает ваш проект при каждом push в GitHub:

1. Убедитесь, что в настройках сервиса включен **"Auto Deploy"**
2. При каждом коммите в main ветку Railway автоматически:
   - Получит новый код
   - Установит зависимости
   - Запустит build
   - Перезапустит сервис

### Шаг 10.2: Проверка деплоя

1. Откройте вкладку **"Deployments"** в сервисе
2. Вы увидите историю всех деплоев
3. Зеленый статус означает успешный деплой
4. Кликнув на деплой, можно посмотреть логи

---

## Часть 11: Настройка переменных окружения для продакшена

### Финальный список переменных для бэкенда:

```
DATABASE_URL=<из PostgreSQL>
JWT_SECRET=<случайная строка>
JWT_EXPIRES_IN=7d
NODE_ENV=production
PORT=5000
FRONTEND_URL=https://ваш-фронтенд-url.railway.app
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
QR_POINT_SECRET=<случайная строка>
```

### Финальный список переменных для фронтенда:

```
REACT_APP_API_URL=https://ваш-бэкенд-url.railway.app
```

---

## Часть 12: Проверка работоспособности

### Шаг 12.1: Проверка бэкенда

1. Откройте URL вашего бэкенда в браузере
2. Добавьте `/api/health` в конце URL
3. Должен вернуться JSON: `{"status":"ok","timestamp":"..."}`

### Шаг 12.2: Проверка фронтенда

1. Откройте URL вашего фронтенда
2. Попробуйте залогиниться
3. Проверьте работу основных функций

---

## Часть 13: Мониторинг и логи

### Просмотр логов в Railway

1. В любом сервисе перейдите на вкладку **"Logs"**
2. Вы увидите логи в реальном времени
3. Можно фильтровать логи по уровню (INFO, ERROR, и т.д.)

### Мониторинг ресурсов

1. Перейдите на вкладку **"Metrics"**
2. Вы увидите:
   - Использование CPU
   - Использование памяти
   - Количество запросов
   - Время ответа

---

## Часть 14: Решение типичных проблем

### Проблема: Бэкенд не запускается

**Решение:**
1. Проверьте логи в Railway
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что миграции базы данных выполнены
4. Убедитесь, что `DATABASE_URL` правильный

### Проблема: CORS ошибки

**Решение:**
1. Проверьте, что `FRONTEND_URL` в бэкенде правильный
2. Убедитесь, что в `backend/src/index.ts` настроен CORS правильно
3. Проверьте, что фронтенд использует правильный `REACT_APP_API_URL`

### Проблема: База данных не подключается

**Решение:**
1. Проверьте `DATABASE_URL` в переменных окружения
2. Убедитесь, что миграции выполнены
3. Проверьте логи бэкенда на наличие ошибок подключения

### Проблема: Фронтенд не может подключиться к бэкенду

**Решение:**
1. Проверьте `REACT_APP_API_URL` во фронтенде
2. Убедитесь, что URL бэкенда правильный (с https://)
3. Проверьте, что бэкенд запущен и доступен
4. Проверьте CORS настройки в бэкенде

---

## Часть 15: Оптимизация и производительность

### Настройка ресурсов

1. В настройках каждого сервиса есть раздел **"Resources"**
2. Можно настроить:
   - Количество RAM
   - Количество CPU
   - Автоматическое масштабирование

### Кеширование

1. Для фронтенда Railway автоматически кеширует статические файлы
2. Для бэкенда можно настроить кеширование в коде

---

## Полезные команды Railway CLI

```bash
# Вход в Railway
railway login

# Связывание с проектом
railway link

# Просмотр переменных окружения
railway variables

# Добавление переменной
railway variables set KEY=value

# Запуск команды в окружении Railway
railway run <команда>

# Просмотр логов
railway logs

# Открытие проекта в браузере
railway open
```

---

## Структура проекта на Railway

После настройки у вас должно быть:

```
Railway Project: typography-erp
├── PostgreSQL Database
│   └── Variables: DATABASE_URL
├── Backend Service
│   ├── Root: backend/
│   ├── Variables: JWT_SECRET, DATABASE_URL, FRONTEND_URL, etc.
│   └── URL: backend-production.up.railway.app
└── Frontend Service
    ├── Root: frontend/
    ├── Variables: REACT_APP_API_URL
    └── URL: frontend-production.up.railway.app
```

---

## Дополнительные рекомендации

1. **Резервное копирование**: Настройте регулярные бэкапы базы данных
2. **Мониторинг**: Используйте встроенные метрики Railway
3. **Логирование**: Настройте централизованное логирование для продакшена
4. **Безопасность**: Используйте сильные секреты для JWT и других ключей
5. **Обновления**: Регулярно обновляйте зависимости

---

## Где найти помощь

- [Railway Documentation](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)
- [Railway Status](https://status.railway.app)

---

## Краткая шпаргалка по шагам

1. ✅ Загрузите проект на GitHub
2. ✅ Зарегистрируйтесь на Railway
3. ✅ Создайте проект и подключите GitHub репозиторий
4. ✅ Создайте PostgreSQL базу данных
5. ✅ Настройте бэкенд сервис (root: backend/)
6. ✅ Настройте переменные окружения для бэкенда
7. ✅ Выполните миграции базы данных
8. ✅ Получите URL бэкенда
9. ✅ Настройте фронтенд сервис (root: frontend/)
10. ✅ Настройте REACT_APP_API_URL для фронтенда
11. ✅ Обновите FRONTEND_URL в бэкенде
12. ✅ Проверьте работоспособность

---

**Удачи с развертыванием!** 🚀

