# Финальная инструкция по настройке Railway

## 🎯 Ваш проект на Railway

**Проект:** Tipograf  
**Ссылка:** https://railway.com/project/20639198-334b-4d20-b9b1-9a3dc6aae135

## 📋 Что нужно сделать (5 минут)

### Шаг 1: Настройка Backend (2 минуты)

**Ссылка:** https://railway.com/project/20639198-334b-4d20-b9b1-9a3dc6aae135/service/f3a60971-64bb-44f9-aedb-b0206e81e4b2/settings

1. **Root Directory:**
   - Прокрутите вниз до секции "Source"
   - Найдите поле "Root Directory"
   - Введите: `backend`
   - Нажмите "Save"

2. **Генерация домена:**
   - Секция "Networking" → "Public Networking"
   - Включите "Generate Domain" или нажмите кнопку
   - Скопируйте URL (запомните его!)

3. **Переменные окружения:**
   - Перейдите во вкладку **"Variables"** (в меню слева или сверху)
   - Добавьте переменные (кнопка "+ New Variable"):

   ```
   DATABASE_URL=<получить из PostgreSQL - см. ниже>
   JWT_SECRET=<случайная строка 32+ символов>
   JWT_EXPIRES_IN=7d
   NODE_ENV=production
   PORT=5000
   FRONTEND_URL=<заполнить после настройки фронтенда>
   UPLOAD_DIR=./uploads
   MAX_FILE_SIZE=10485760
   QR_POINT_SECRET=<случайная строка 32+ символов>
   ```

   **Генерация секретов** (PowerShell):
   ```powershell
   [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
   ```
   Выполните 2 раза - один для JWT_SECRET, один для QR_POINT_SECRET.

### Шаг 2: Получение DATABASE_URL (1 минута)

1. Вернитесь на главную страницу проекта
2. Найдите карточку **"Postgre..."** (PostgreSQL база данных)
3. Кликните на неё
4. Перейдите во вкладку **"Variables"**
5. Найдите `DATABASE_URL` или `POSTGRES_URL`
6. Скопируйте значение
7. Вставьте в переменную `DATABASE_URL` в Backend сервисе (шаг 1.3)

### Шаг 3: Настройка Frontend (2 минуты)

**Как найти сервис:**
- На главной странице проекта найдите карточку **"Site"**
- Кликните на неё
- Перейдите в **"Settings"**

1. **Root Directory:**
   - Секция "Source" → "Root Directory"
   - Введите: `frontend`
   - Сохраните

2. **Генерация домена:**
   - Секция "Networking" → "Public Networking"
   - Включите "Generate Domain"
   - Скопируйте URL (запомните его!)

3. **Переменная окружения:**
   - Вкладка **"Variables"**
   - Добавьте:
   ```
   REACT_APP_API_URL=https://<URL-вашего-бэкенда>/api
   ```
   Например: `REACT_APP_API_URL=https://handsome-serenity-production.up.railway.app/api`

### Шаг 4: Обновление FRONTEND_URL (30 секунд)

1. Вернитесь к Backend сервису (handsome-serenity)
2. Перейдите в **"Variables"**
3. Найдите `FRONTEND_URL`
4. Обновите значение на URL вашего фронтенда
   - Например: `FRONTEND_URL=https://site-production.up.railway.app`

### Шаг 5: Ожидание сборки (2-5 минут)

Railway автоматически пересоберет сервисы после изменений.

1. Откройте вкладку **"Deployments"** в каждом сервисе
2. Дождитесь успешной сборки (зеленый статус)
3. Если сборка не удалась - проверьте логи

## ✅ Проверка работоспособности

### Backend:
Откройте в браузере:
```
https://<ваш-бэкенд-url>/api/health
```

Должен вернуться:
```json
{"status":"ok","timestamp":"..."}
```

### Frontend:
Откройте в браузере:
```
https://<ваш-фронтенд-url>
```

Должна загрузиться главная страница.

## 🔗 Полезные ссылки

- **Проект:** https://railway.com/project/20639198-334b-4d20-b9b1-9a3dc6aae135
- **Backend Settings:** https://railway.com/project/20639198-334b-4d20-b9b1-9a3dc6aae135/service/f3a60971-64bb-44f9-aedb-b0206e81e4b2/settings
- **Frontend:** Найдите сервис "Site" на главной странице

## ⚠️ Если что-то не работает:

1. **Сборка падает:**
   - Проверьте, что Root Directory установлен правильно
   - Проверьте логи в разделе "Deployments"

2. **Backend не запускается:**
   - Проверьте, что все переменные окружения установлены
   - Проверьте, что DATABASE_URL правильный (публичный, не внутренний)

3. **Frontend не подключается к API:**
   - Проверьте `REACT_APP_API_URL` (должен начинаться с `https://`)
   - Проверьте, что бэкенд запущен и доступен

4. **CORS ошибки:**
   - Проверьте, что `FRONTEND_URL` в Backend правильный

---

**После выполнения всех шагов ваш сайт будет доступен всем пользователям! 🎉**

