# Быстрая настройка Railway - Пошаговая инструкция

## 🎯 Цель
Настроить проект так, чтобы сайт был доступен всем пользователям.

## ⚡ Быстрый способ (через веб-интерфейс)

### 1️⃣ Настройка Backend ("handsome-serenity")

1. **Откройте сервис:**
   - Перейдите: https://railway.com/project/20639198-334b-4d20-b9b1-9a3dc6aae135/service/f3a60971-64bb-44f9-aedb-b0206e81e4b2/settings

2. **Установите Root Directory:**
   - Прокрутите до секции "Source"
   - Найдите "Root Directory"
   - Введите: `backend`
   - Нажмите "Save" или "Update"

3. **Настройте домен:**
   - Прокрутите до секции "Networking"
   - Найдите "Public Networking"
   - Нажмите "Generate Domain" или включите переключатель
   - Скопируйте полученный URL (например: `handsome-serenity-production.up.railway.app`)

4. **Добавьте переменные окружения:**
   - Перейдите во вкладку "Variables" (рядом с Settings)
   - Нажмите "+ New Variable" и добавьте каждую переменную:

   ```
   DATABASE_URL=<см. шаг 5>
   JWT_SECRET=<генерируйте случайную строку 32+ символов>
   JWT_EXPIRES_IN=7d
   NODE_ENV=production
   PORT=5000
   FRONTEND_URL=<будет заполнено позже>
   UPLOAD_DIR=./uploads
   MAX_FILE_SIZE=10485760
   QR_POINT_SECRET=<генерируйте случайную строку 32+ символов>
   ```

5. **Получите DATABASE_URL:**
   - Вернитесь на главную страницу проекта
   - Найдите сервис PostgreSQL
   - Кликните на него
   - Перейдите во вкладку "Variables"
   - Скопируйте значение `DATABASE_URL` или `POSTGRES_URL`
   - Вставьте его в переменную `DATABASE_URL` в Backend сервисе

### 2️⃣ Настройка Frontend ("Site")

1. **Откройте сервис:**
   - Найдите карточку "Site" на главной странице проекта
   - Кликните на неё
   - Перейдите в "Settings"

2. **Установите Root Directory:**
   - В секции "Source" найдите "Root Directory"
   - Введите: `frontend`
   - Сохраните

3. **Настройте домен:**
   - В секции "Networking" → "Public Networking"
   - Включите "Generate Domain"
   - Скопируйте URL (например: `site-production.up.railway.app`)

4. **Добавьте переменную окружения:**
   - Перейдите во вкладку "Variables"
   - Добавьте:
   ```
   REACT_APP_API_URL=https://<URL-вашего-бэкенда>/api
   ```
   Например: `REACT_APP_API_URL=https://handsome-serenity-production.up.railway.app/api`

### 3️⃣ Обновление FRONTEND_URL в Backend

1. Вернитесь к сервису "handsome-serenity"
2. Перейдите в "Variables"
3. Обновите `FRONTEND_URL` на URL вашего фронтенда
   - Например: `FRONTEND_URL=https://site-production.up.railway.app`

### 4️⃣ Проверка

После настройки Railway автоматически пересоберет сервисы. Проверьте:

1. **Backend:**
   - Откройте URL бэкенда + `/api/health`
   - Должен вернуться: `{"status":"ok"}`

2. **Frontend:**
   - Откройте URL фронтенда
   - Попробуйте залогиниться

## 🔧 Генерация секретов

Для `JWT_SECRET` и `QR_POINT_SECRET` используйте:

**Windows PowerShell:**
```powershell
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
```

**Онлайн генератор:**
https://randomkeygen.com/

## ⚠️ Критически важно:

1. ✅ **Root Directory должен быть установлен:** `backend` для бэкенда, `frontend` для фронтенда
2. ✅ **Все URL должны начинаться с `https://`**
3. ✅ **DATABASE_URL должен быть публичным** (не `postgres.railway.internal`)
4. ✅ **Миграции выполнятся автоматически** при первом запуске

## 📝 Чеклист:

- [ ] Root Directory установлен для Backend (`backend`)
- [ ] Root Directory установлен для Frontend (`frontend`)
- [ ] Домен сгенерирован для Backend
- [ ] Домен сгенерирован для Frontend
- [ ] Все переменные окружения добавлены в Backend
- [ ] `REACT_APP_API_URL` добавлен в Frontend
- [ ] `FRONTEND_URL` обновлен в Backend
- [ ] `DATABASE_URL` установлен из PostgreSQL сервиса
- [ ] Секреты (`JWT_SECRET`, `QR_POINT_SECRET`) сгенерированы
- [ ] Backend собирается успешно
- [ ] Frontend собирается успешно
- [ ] `/api/health` возвращает `{"status":"ok"}`
- [ ] Frontend загружается в браузере

---

**После выполнения всех пунктов ваш сайт будет доступен всем пользователям! 🚀**

