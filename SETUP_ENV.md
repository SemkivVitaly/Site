# ⚠️ СРОЧНО: Настройка переменных окружения

## ❌ Ошибка: DATABASE_URL не установлена!

Ваше приложение не может запуститься, потому что не установлены необходимые переменные окружения.

## 🔧 Что нужно сделать СЕЙЧАС:

### 1. Перейдите в настройки вашей платформы деплоя:

**Railway:**
- Откройте ваш проект
- Перейдите в Settings → Variables
- Добавьте все переменные ниже

**Render:**
- Откройте ваш сервис
- Перейдите в Environment
- Добавьте все переменные

**Heroku:**
- Используйте команду `heroku config:set` или панель управления

### 2. Добавьте ОБЯЗАТЕЛЬНЫЕ переменные:

```env
DATABASE_URL=postgresql://user:password@host:5432/database?schema=public
```

**ВАЖНО:** Эта переменная обычно создается автоматически при подключении PostgreSQL. Проверьте:
- PostgreSQL сервис подключен?
- DATABASE_URL появился в списке переменных?

### 3. Добавьте остальные переменные:

```env
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа-сгенерируйте-случайный
JWT_EXPIRES_IN=7d
FRONTEND_URL=https://ваш-фронтенд-домен.com
QR_POINT_SECRET=ваш-qr-секрет-минимум-32-символа
NODE_ENV=production
PORT=5000
```

## 🔐 Как сгенерировать секретные ключи:

Вы можете использовать любой из способов:

**Через Node.js:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

**Через OpenSSL:**
```bash
openssl rand -hex 32
```

**Онлайн генератор:**
- https://generate-secret.vercel.app/32

## ✅ После добавления переменных:

1. Сохраните изменения
2. Платформа автоматически перезапустит контейнер
3. Проверьте логи - должно появиться:
   ```
   ✅ Server running on 0.0.0.0:5000
   ```

## 🔍 Проверка:

После настройки проверьте endpoint:
```
GET https://ваш-api.com/api/health
```

Должен вернуть:
```json
{"status":"ok","timestamp":"..."}
```

## 📋 Чеклист:

- [ ] PostgreSQL сервис подключен
- [ ] DATABASE_URL установлен (проверьте автоматически созданную переменную)
- [ ] JWT_SECRET установлен (минимум 32 символа)
- [ ] FRONTEND_URL установлен (ваш реальный домен)
- [ ] QR_POINT_SECRET установлен (минимум 32 символа)
- [ ] NODE_ENV=production
- [ ] Приложение перезапущено
- [ ] Health check работает

## 🆘 Если проблема осталась:

1. Проверьте логи деплоя
2. Убедитесь, что все переменные сохранены
3. Убедитесь, что контейнер перезапущен после изменения переменных
4. Проверьте формат DATABASE_URL (должен начинаться с `postgresql://`)

