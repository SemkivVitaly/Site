# Полная инструкция по настройке проекта на Railway

Эта инструкция поможет вам полностью настроить проект для деплоя на Railway, чтобы сайт был доступен всем пользователям.

## 📋 Содержание

1. [Создание проекта на Railway](#1-создание-проекта-на-railway)
2. [Настройка базы данных PostgreSQL](#2-настройка-базы-данных-postgresql)
3. [Настройка Backend сервиса](#3-настройка-backend-сервиса)
4. [Настройка Frontend сервиса](#4-настройка-frontend-сервиса)
5. [Проверка работоспособности](#5-проверка-работоспособности)
6. [Решение проблем](#6-решение-проблем)

---

## 1. Создание проекта на Railway

### Шаг 1.1: Регистрация и вход

1. Перейдите на [railway.app](https://railway.app)
2. Войдите через GitHub (если у вас нет аккаунта, создайте его)
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий с проектом

### Шаг 1.2: Структура проекта

После подключения репозитория Railway автоматически обнаружит проект. Вам нужно будет создать 3 сервиса:
- **PostgreSQL** (база данных)
- **Backend** (API сервер)
- **Frontend** (React приложение)

---

## 2. Настройка базы данных PostgreSQL

### Шаг 2.1: Создание PostgreSQL сервиса

1. На главной странице проекта нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных

### Шаг 2.2: Получение DATABASE_URL

1. Кликните на карточку **PostgreSQL** в вашем проекте
2. Перейдите во вкладку **"Variables"**
3. Найдите переменную **`DATABASE_URL`** или **`POSTGRES_URL`**
4. Скопируйте значение (оно понадобится для Backend)

**Важно:** Используйте **публичный** URL (обычно начинается с `postgresql://`), а не внутренний.

---

## 3. Настройка Backend сервиса

### Шаг 3.1: Создание Backend сервиса

1. На главной странице проекта нажмите **"+ New"**
2. Выберите **"GitHub Repo"** (если еще не подключен) или **"Empty Service"**
3. Если выбрали Empty Service, подключите ваш GitHub репозиторий

### Шаг 3.2: Настройка Root Directory

1. Кликните на сервис Backend
2. Перейдите в **"Settings"**
3. Прокрутите до секции **"Source"**
4. Найдите поле **"Root Directory"**
5. Введите: `backend`
6. Нажмите **"Save"**

### Шаг 3.3: Генерация публичного домена

1. В том же разделе Settings найдите секцию **"Networking"**
2. Найдите **"Public Networking"**
3. Включите **"Generate Domain"** или нажмите кнопку **"Generate Domain"**
4. Скопируйте URL (например: `https://your-backend-name.up.railway.app`)
5. **Запомните этот URL** - он понадобится для Frontend

### Шаг 3.4: Настройка переменных окружения

1. Перейдите во вкладку **"Variables"** (в меню слева или сверху)
2. Нажмите **"+ New Variable"** для каждой переменной:

#### Обязательные переменные:

```env
# База данных (скопируйте из PostgreSQL сервиса)
DATABASE_URL=postgresql://user:password@host:port/database?schema=public

# JWT секреты (сгенерируйте случайные строки)
JWT_SECRET=ваш-случайный-секрет-32-символа-минимум
JWT_EXPIRES_IN=7d

# Сервер
PORT=5000
NODE_ENV=production

# CORS (заполните после настройки Frontend)
FRONTEND_URL=https://your-frontend-url.up.railway.app

# Загрузка файлов
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# QR коды
QR_POINT_SECRET=ваш-случайный-секрет-32-символа-минимум
```

#### Генерация секретов (PowerShell):

```powershell
# Для JWT_SECRET
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))

# Для QR_POINT_SECRET (выполните еще раз)
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
```

**Или используйте онлайн генератор:** [randomkeygen.com](https://randomkeygen.com/)

### Шаг 3.5: Проверка конфигурации

Убедитесь, что файлы существуют:
- ✅ `backend/nixpacks.toml` - конфигурация сборки
- ✅ `backend/railway.json` - конфигурация Railway
- ✅ `backend/package.json` - зависимости

Railway автоматически начнет сборку после сохранения переменных.

---

## 4. Настройка Frontend сервиса

### Шаг 4.1: Создание Frontend сервиса

1. На главной странице проекта нажмите **"+ New"**
2. Выберите **"GitHub Repo"** (если еще не подключен) или **"Empty Service"**
3. Если выбрали Empty Service, подключите ваш GitHub репозиторий

### Шаг 4.2: Настройка Root Directory

1. Кликните на сервис Frontend
2. Перейдите в **"Settings"**
3. Прокрутите до секции **"Source"**
4. Найдите поле **"Root Directory"**
5. Введите: `frontend`
6. Нажмите **"Save"**

### Шаг 4.3: Генерация публичного домена

1. В том же разделе Settings найдите секцию **"Networking"**
2. Найдите **"Public Networking"**
3. Включите **"Generate Domain"** или нажмите кнопку **"Generate Domain"**
4. Скопируйте URL (например: `https://your-frontend-name.up.railway.app`)
5. **Запомните этот URL**

### Шаг 4.4: Настройка переменных окружения

1. Перейдите во вкладку **"Variables"**
2. Нажмите **"+ New Variable"**

#### Обязательная переменная:

```env
REACT_APP_API_URL=https://your-backend-url.up.railway.app
```

**Важно:** 
- Используйте URL вашего Backend сервиса (без `/api` в конце)
- URL должен начинаться с `https://`
- Socket.io автоматически уберет `/api` из URL при подключении

**Пример:**
```env
REACT_APP_API_URL=https://handsome-serenity-production.up.railway.app
```

### Шаг 4.5: Обновление FRONTEND_URL в Backend

1. Вернитесь к Backend сервису
2. Перейдите в **"Variables"**
3. Найдите переменную `FRONTEND_URL`
4. Обновите значение на URL вашего Frontend сервиса:

```env
FRONTEND_URL=https://your-frontend-url.up.railway.app
```

**Пример:**
```env
FRONTEND_URL=https://site-production.up.railway.app
```

### Шаг 4.6: Проверка конфигурации

Убедитесь, что файлы существуют:
- ✅ `frontend/nixpacks.toml` - конфигурация сборки
- ✅ `frontend/railway.json` - конфигурация Railway
- ✅ `frontend/package.json` - зависимости

---

## 5. Проверка работоспособности

### Шаг 5.1: Ожидание сборки

Railway автоматически пересоберет сервисы после изменений:

1. Откройте вкладку **"Deployments"** в каждом сервисе
2. Дождитесь успешной сборки (зеленый статус ✅)
3. Если сборка не удалась - проверьте логи (кликните на deployment)

### Шаг 5.2: Проверка Backend

Откройте в браузере:
```
https://your-backend-url.up.railway.app/api/health
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "timestamp": "2024-01-01T12:00:00.000Z"
}
```

Если видите этот ответ - Backend работает! ✅

### Шаг 5.3: Проверка Frontend

Откройте в браузере:
```
https://your-frontend-url.up.railway.app
```

**Ожидаемый результат:**
- Загружается главная страница
- Можно залогиниться (если есть пользователи)
- Нет ошибок в консоли браузера (F12)

### Шаг 5.4: Проверка подключения Frontend к Backend

1. Откройте консоль браузера (F12)
2. Перейдите на страницу входа
3. Проверьте Network tab - должны быть запросы к `/api/auth/login` или подобным
4. Не должно быть CORS ошибок

---

## 6. Решение проблем

### Проблема: Сборка Backend падает

**Возможные причины:**
1. Неправильный Root Directory
2. Отсутствуют переменные окружения
3. Ошибки в коде

**Решение:**
1. Проверьте, что Root Directory = `backend`
2. Проверьте все переменные окружения (особенно `DATABASE_URL`)
3. Откройте логи сборки (Deployments → кликните на failed deployment)
4. Убедитесь, что `backend/nixpacks.toml` существует

### Проблема: Сборка Frontend падает

**Возможные причины:**
1. Неправильный Root Directory
2. Ошибки компиляции TypeScript/React
3. Проблемы с зависимостями (ajv, ajv-formats)

**Решение:**
1. Проверьте, что Root Directory = `frontend`
2. Проверьте логи сборки
3. Убедитесь, что версии зависимостей правильные:
   - `ajv@^6.12.6`
   - `ajv-formats@^1.5.1`
   - `ajv-keywords@^3.5.2`
4. Убедитесь, что `frontend/nixpacks.toml` существует

### Проблема: Backend не запускается

**Возможные причины:**
1. Неправильный `DATABASE_URL`
2. База данных не доступна
3. Отсутствуют обязательные переменные

**Решение:**
1. Проверьте `DATABASE_URL` - должен быть публичный URL
2. Проверьте, что PostgreSQL сервис запущен
3. Проверьте все переменные окружения
4. Откройте логи (Deployments → Running → View Logs)

### Проблема: Frontend не подключается к API

**Возможные причины:**
1. Неправильный `REACT_APP_API_URL`
2. Backend не запущен
3. CORS ошибки

**Решение:**
1. Проверьте `REACT_APP_API_URL`:
   - Должен начинаться с `https://`
   - Должен быть URL вашего Backend (без `/api`)
   - Пример: `https://your-backend.up.railway.app`
2. Проверьте, что Backend запущен (см. проверку выше)
3. Проверьте `FRONTEND_URL` в Backend - должен совпадать с URL Frontend
4. Откройте консоль браузера (F12) и проверьте ошибки

### Проблема: CORS ошибки

**Решение:**
1. Убедитесь, что `FRONTEND_URL` в Backend правильный
2. URL должен быть полным: `https://your-frontend-url.up.railway.app`
3. Без завершающего слэша
4. Перезапустите Backend после изменения переменной

### Проблема: Socket.io не подключается

**Решение:**
1. Socket.io использует тот же URL, что и API, но без `/api`
2. Убедитесь, что `REACT_APP_API_URL` правильный
3. Проверьте, что Backend запущен и Socket.io работает
4. Откройте консоль браузера - должны быть сообщения о подключении

### Проблема: База данных не подключается

**Решение:**
1. Проверьте, что PostgreSQL сервис запущен
2. Используйте **публичный** `DATABASE_URL`, а не внутренний
3. Убедитесь, что миграции выполнены (Railway должен выполнить их автоматически)
4. Проверьте логи Backend для ошибок подключения

---

## 📝 Чек-лист перед деплоем

Перед тем как считать настройку завершенной, убедитесь:

### Backend:
- [ ] Root Directory = `backend`
- [ ] Публичный домен сгенерирован
- [ ] Все переменные окружения установлены:
  - [ ] `DATABASE_URL` (из PostgreSQL)
  - [ ] `JWT_SECRET`
  - [ ] `JWT_EXPIRES_IN=7d`
  - [ ] `NODE_ENV=production`
  - [ ] `PORT=5000`
  - [ ] `FRONTEND_URL` (URL Frontend)
  - [ ] `UPLOAD_DIR=./uploads`
  - [ ] `MAX_FILE_SIZE=10485760`
  - [ ] `QR_POINT_SECRET`
- [ ] Сборка успешна (зеленый статус)
- [ ] `/api/health` возвращает `{"status":"ok"}`

### Frontend:
- [ ] Root Directory = `frontend`
- [ ] Публичный домен сгенерирован
- [ ] Переменная `REACT_APP_API_URL` установлена (URL Backend без `/api`)
- [ ] Сборка успешна (зеленый статус)
- [ ] Страница загружается без ошибок

### База данных:
- [ ] PostgreSQL сервис создан
- [ ] `DATABASE_URL` скопирован в Backend

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен всем пользователям по URL вашего Frontend сервиса!

**Следующие шаги:**
1. Создайте первого пользователя через API или Prisma Studio
2. Протестируйте все функции приложения
3. Настройте кастомный домен (опционально) в Settings → Networking

---

## 📞 Полезные ссылки

- [Railway Documentation](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)
- [Ваш проект на Railway](https://railway.com/project/20639198-334b-4d20-b9b1-9a3dc6aae135)

---

**Примечание:** Если у вас возникли проблемы, не описанные в этом руководстве, проверьте логи в разделе Deployments каждого сервиса.

