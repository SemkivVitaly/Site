[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "npm install --legacy-peer-deps",
  "npm install ajv@^8.12.0 ajv-keywords@^5.1.0 ajv-formats@^2.1.1 --legacy-peer-deps --save"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

