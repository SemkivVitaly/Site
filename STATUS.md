# Статус проекта

## ✅ Backend
- **Статус**: Запущен и работает
- **URL**: http://localhost:5000
- **Health Check**: http://localhost:5000/api/health ✅
- **Порт**: 5000

## 🔄 Frontend
- **Статус**: Запускается
- **URL**: http://localhost:3000 (откроется автоматически)
- **Порт**: 3000

## 📋 Следующие шаги

### 1. Настройка базы данных (ОБЯЗАТЕЛЬНО)

PostgreSQL должен быть установлен и запущен:

```sql
CREATE DATABASE typography_erp;
```

Обновите `backend/.env`:
```env
DATABASE_URL="postgresql://username:password@localhost:5432/typography_erp?schema=public"
```

### 2. Выполнение миграций

```bash
cd backend
npx prisma generate
npx prisma migrate dev --name init
```

### 3. Создание первого пользователя

Через Prisma Studio:
```bash
cd backend
npx prisma studio
```

Или через API (после запуска):
```bash
POST http://localhost:5000/api/auth/register
Content-Type: application/json

{
  "email": "admin@typography.ru",
  "password": "admin123",
  "firstName": "Администратор",
  "lastName": "Системы",
  "role": "ADMIN"
}
```

## 🔧 Решение проблем

### Frontend не запускается
- Проверьте, что порт 3000 свободен
- Убедитесь, что все зависимости установлены: `npm install --legacy-peer-deps`

### Backend не подключается к БД
- Проверьте, что PostgreSQL запущен
- Убедитесь, что база данных создана
- Проверьте данные в `backend/.env`

### Ошибки компиляции
- Удалите `node_modules` и переустановите: `npm install --legacy-peer-deps`
- Проверьте версию Node.js (рекомендуется 18+)

## 📚 Документация
- Подробная инструкция: `SETUP.md`
- Быстрый старт: `QUICK_START.md`

